"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
describe('convertConfData', function () {
    it('sets the talk name as all text preceding the first numeral', function () {
        expect.hasAssertions();
    });
    it("sets talk length to 5 if 'lightning' is found", function () {
        expect.hasAssertions();
    });
    it("converts a 'NNmin' string to a NN numeral", function () {
        expect.hasAssertions();
    });
    it("sets the converted numeral to the talk length", function () {
        expect.hasAssertions();
    });
    it("return an array of all talks", function () {
        expect.hasAssertions();
    });
});
