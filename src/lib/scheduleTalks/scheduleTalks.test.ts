import { scheduleTalks } from './scheduleTalks';

describe('scheduleTalks', () => {
      
  it('checks if total talk length exceeds total available time', () => {
    expect.hasAssertions();
  });

});

